using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MudBulletEffectHandler : MonoBehaviour
{
    public void ApplyEffect(Collider2D collision, BulletController bullet)
    {
        
        //var tile = collision.GetComponent<UnitTile>();
        //if (tile != null && tile.Mud == TileType.Ice)
        //{
        //    tile.ChangeType(TileType.Mud); // hypothetical method
        //    Debug.Log("[MUD BULLET] Changed Ice tile to Mud");
        //}
    }
}
